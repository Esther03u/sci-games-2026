# Normalise line endings: LF in the repo and in every working copy.
# (Windows clones with core.autocrlf=true were warning on every commit.)
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
